// Code generated by protoc-gen-go-crud. DO NOT EDIT.
// source: test-cases/created-at/test.proto

/*
Package created_at is a repository.

SQLite implementation.
*/

package created_at

import (
	"context"
	"database/sql"
	"fmt"
	"strings"
	"time"

	"google.golang.org/protobuf/types/known/timestamppb"
	"modernc.org/sqlite"
	"github.com/samlitowitz/expressions"
)

// InMemoryCreatedAtRepository is an in memory implementation of the CreatedAtRepository interface.
type SQLiteCreatedAtRepository struct {
	db *sql.DB
}

// NewInMemory creates a new InMemoryCreatedAtRepository to be used.
func NewSQLiteCreatedAtRepository(db *sql.DB) (*SQLiteCreatedAtRepository, error) {
	_, ok := db.Driver().(*sqlite.Driver)
	if !ok {
		return nil, fmt.Errorf("invalid driver, must be of type *modernc.org/sqlite.Driver")
	}
	return &SQLiteCreatedAtRepository{
		db: db,
	}, nil
}

// Create creates new CreatedAts.
// Successfully created CreatedAts are returned along with any errors that may have occurred.
func (repo *SQLiteCreatedAtRepository) Create(ctx context.Context, toCreate []*CreatedAt) ([]*CreatedAt, error) {
	if len(toCreate) == 0 {
		return nil, nil
	}
	tx, err := repo.db.BeginTx(ctx, &sql.TxOptions{Isolation: sql.LevelSerializable})
	if err != nil {
		return nil, err
	}
	defer tx.Rollback()

	for _, createdAt := range toCreate {
		if createdAt.GetCreatedAt() != nil {
			continue
		}
		createdAt.SetCreatedAt(timestamppb.New(time.Now()))
	}
	binds := []any{}
	bindsStrs := []string{}
	for _, createdAt := range toCreate {
		binds = append(binds, createdAt.GetId())
		binds = append(binds, createdAt.GetData())
		binds = append(binds, createdAt.GetCreatedAt().AsTime().Format(time.RFC3339))
		bindsStrs = append(bindsStrs, "(?,?,?)")
	}
	_, err = tx.ExecContext(
		ctx,
		fmt.Sprintf(
			`INSERT INTO "created_at" ("id","data","created_at") VALUES
			%s`,
			strings.Join(bindsStrs, ",\n"),
		),
		binds...,
	)
	if err != nil {
		return nil, err
	}
	err = tx.Commit()
	if err != nil {
		return nil, err
	}
	return toCreate, nil
}

// Read returns a set of CreatedAts matching the provided criteria
// Read is incomplete and it should be considered unstable
func (repo *SQLiteCreatedAtRepository) Read(ctx context.Context, expr expressions.Expression) ([]*CreatedAt, error) {
	query := `SELECT "id","data","created_at"
		FROM "created_at"`
	clauses, binds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr)
	if err != nil {
		return nil, err
	}
	if clauses != "" {
		query += "\nWHERE\n" + clauses
	}
	stmt, err := repo.db.Prepare(query)
	if err != nil {
		return nil, err
	}
	rows, err := stmt.QueryContext(ctx, binds...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var found []*CreatedAt
	for rows.Next() {
		createdAt := &CreatedAt_builder{}
		var createdAtTimeStr string

		if err = rows.Scan(&createdAt.Id, &createdAt.Data, &createdAtTimeStr); err != nil {
			return nil, err
		}

		createdAtTime, err := time.Parse(time.RFC3339, createdAtTimeStr)
		if err != nil {
			return nil, err
		}
		createdAt.CreatedAt = timestamppb.New(createdAtTime)

		found = append(found, createdAt.Build())
	}
	if err = rows.Err(); err != nil {
		return nil, err
	}
	return found, nil
}

// Update modifies existing CreatedAts based on the defined unique identifiers.
func (repo *SQLiteCreatedAtRepository) Update(ctx context.Context, toUpdate []*CreatedAt) ([]*CreatedAt, error) {
	if len(toUpdate) == 0 {
		return nil, nil
	}
	tx, err := repo.db.BeginTx(ctx, &sql.TxOptions{Isolation: sql.LevelSerializable})
	if err != nil {
		return nil, err
	}
	defer tx.Rollback()

	stmt, err := tx.Prepare(
		`UPDATE "created_at" SET "data" = ?,"created_at" = ? WHERE "id" = ?`,
	)
	if err != nil {
		return nil, err
	}
	defer stmt.Close()

	for _, createdAt := range toUpdate {
		_, err = stmt.ExecContext(ctx, createdAt.GetData(), createdAt.GetCreatedAt().AsTime().Format(time.RFC3339), createdAt.GetId())
		if err != nil {
			return nil, err
		}
	}
	err = tx.Commit()
	if err != nil {
		return nil, err
	}
	return toUpdate, nil
}

// Delete deletes CreatedAts based on the defined unique identifiers
func (repo *SQLiteCreatedAtRepository) Delete(ctx context.Context, expr expressions.Expression) error {
	query := `DELETE FROM "created_at"`
	clauses, binds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr)
	if err != nil {
		return err
	}
	if clauses != "" {
		query += "\nWHERE\n" + clauses
	}
	stmt, err := repo.db.Prepare(query)
	if err != nil {
		return err
	}
	_, err = stmt.ExecContext(ctx, binds...)
	if err != nil {
		return err
	}
	return nil
}

var sqliteCreatedAtColumnNameByFieldID = map[expressions.ID]string{
	CreatedAt_Id_Field:        "id",
	CreatedAt_Data_Field:      "data",
	CreatedAt_CreatedAt_Field: "created_at",
}

func whereClauseFromExpressionForSQLiteCreatedAt(expr expressions.Expression) (string, []any, error) {
	if expr == nil {
		return "", nil, nil
	}
	switch expr := expr.(type) {
	case *expressions.And:
		left, leftBinds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr.Left())
		if err != nil {
			return "", nil, err
		}
		right, rightBinds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr.Right())
		if err != nil {
			return "", nil, err
		}
		return fmt.Sprintf("%s AND %s", left, right), append(leftBinds, rightBinds...), nil

	case *expressions.Or:
		left, leftBinds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr.Left())
		if err != nil {
			return "", nil, err
		}
		right, rightBinds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr.Right())
		if err != nil {
			return "", nil, err
		}
		return fmt.Sprintf("%s OR %s", left, right), append(leftBinds, rightBinds...), nil
	case *expressions.Not:
		operand, binds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr.Operand())
		if err != nil {
			return "", nil, err
		}
		return fmt.Sprintf("NOT %s", operand), binds, nil

	case *expressions.Equals:
		left, leftBinds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr.Left())
		if err != nil {
			return "", nil, err
		}
		right, rightBinds, err := whereClauseFromExpressionForSQLiteCreatedAt(expr.Right())
		if err != nil {
			return "", nil, err
		}
		return fmt.Sprintf("%s = %s", left, right), append(leftBinds, rightBinds...), nil

	case *expressions.Identifier:
		if _, ok := validCreatedAtFields[expr.ID()]; !ok {
			return "", nil, fmt.Errorf("invalid field id: %s", expr.ID())
		}
		colName, ok := sqliteCreatedAtColumnNameByFieldID[expr.ID()]
		if !ok {
			return "", nil, fmt.Errorf("missing meta-data: field id: %s", expr.ID())
		}
		return fmt.Sprintf(`"created_at"."%s"`, colName), nil, nil
	case *expressions.Scalar:
		return "?", []any{expr.Value()}, nil
	case expressions.Timestamp:
		return "?", []any{time.Time(expr).Format(time.RFC3339)}, nil
	default:
		return "", nil, fmt.Errorf("unknown expression")
	}
}
